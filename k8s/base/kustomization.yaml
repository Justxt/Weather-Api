apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: weather-api

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml

images:
  - name: weather-api
    newName: weather-api
    newTag: "1.0.0"
